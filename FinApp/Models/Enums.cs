namespace FinApp.Models
{
    public enum UserRole
    {
        USER,
        ADMIN,
        SUPERADMIN,
    }

    public enum LicenseKeyType
    {
        BASIC,
        PREMIUM,
        ENTERPRISE,
    }
}
